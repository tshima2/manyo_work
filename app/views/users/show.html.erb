<!-- <p id="notice"><%= notice %></p> -->
<h1><%= t('users.label_title_mypage', name: @user.name) %></h1>
<table>
  <caption>登録中のタスク件数:<%= @user.tasks.count %>件</caption>
  <tr><td><%= t('users.label_email') %>: <%= @user.email %></td></tr>
</table>

<%= link_to t('users.label_edit')+' | ', edit_user_path(@user) if (current_user && (@user.id == current_user.id)) %>
<%= link_to t('users.label_task_index'), tasks_path %>
